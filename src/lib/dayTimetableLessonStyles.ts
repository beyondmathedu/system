/**
 * @deprecated Use settings from Supabase `app_day_timetable_settings` (edit on Daily/Regular timetable pages).
 * Types & defaults live in `dayTimetableStyleSettings.ts`.
 */

export type {
  DayTimetableFeePaymentTone,
  DayTimetableStyleSettings,
} from "./dayTimetableStyleSettings";
export { DEFAULT_DAY_TIMETABLE_STYLE } from "./dayTimetableStyleSettings";
